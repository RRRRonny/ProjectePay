<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chao117.projectepay">

    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.LoginActivity" />
        <activity android:name=".view.activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".view.activity.PublishActivity" />
        <activity android:name=".test.view.TestActivity" />
        <activity android:name=".test.view.TestGoodsDetailActivity" />
        <activity android:name=".test.view.TestSimpleActivity" />
        <activity android:name=".view.activity.MeActivity" />
        <activity android:name=".view.activity.AddItemActivity" />
        <activity android:name=".view.activity.GeneralListViewActivity" />
        <activity android:name=".view.activity.AccountInfoActivity"></activity>
    </application>

</manifest>